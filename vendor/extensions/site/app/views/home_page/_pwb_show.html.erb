<%= render_snippet('pwb-homepage-header') %>

<hr />
<div class="products">
  <h1><a href="#" title="Top Selling Dog Supplies">Top Selling Dog Supplies</a></h1>
  <ul class="productList">
    <% @best_selling_dogs.each do |product| %>
      <%= render "product", :product => product %>
    <% end %>
  </ul>
  <h1><a href="#" title="Top Selling Cat Supplies">Top Selling Cat Supplies</a></h1>
  <ul class="productList">
    <% @best_selling_cats.each do |product| %>
      <%= render "product", :product => product %>
    <% end %>
  </ul>
</div>

<%= render_snippet('pwb-homepage-footer') %>